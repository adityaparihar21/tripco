{
  "name": "tripco",
  "version": "1.0.0",
  "description": "TripCo static site",
  "scripts": {
    "dev": "npx live-server --watch=.",
    "vercel-build": "mkdir -p dist && cp *.html *.js *.css *.png *.json dist/ || true"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
