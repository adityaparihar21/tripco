{
  "name": "tripco",
  "version": "1.0.0",
  "description": "TripCo static site",
  "scripts": {
    "dev": "npx live-server --watch=.",
    "vercel-build": "echo 'No build required for vanilla static files'"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
